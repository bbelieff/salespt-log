# 세일즈PT 영업일지 — 작업 지침

제품·데이터·아키텍처는 `CLAUDE.md`, 작업 방식은 이 파일을 읽는다. Codex와 Claude에 같은 원칙을 적용한다. 현재 사용자 요청과 실제 실행 환경을 기준으로 일하며, 과거 문서의 역할명·세션 번호·외부 절대 경로를 착수 조건으로 삼지 않는다.

## 시작과 범위

- 요청의 목적·변경 범위·완료 조건을 확인하고 관련 코드, 최근 변경, 필요한 설계 문서만 읽는다. 조사 결과와 추측을 구분한다.
- 이어받는 작업은 `docs/worklog.md`의 관련 최근 기록과 기존 카드·PR을 확인해 중복을 피한다. 별도 임명이나 슬롯 선언 없이 사용자가 요청한 일을 진행한다.
- 코드 변경은 최신 `origin/master`에서 만든 전용 worktree와 의미 있는 브랜치에서 한다. 공유 checkout, 다른 세션의 브랜치·미커밋 변경을 임의로 수정하지 않는다.
- 병렬 작업에서는 파일 소유 범위와 의존성을 확인한다. 공용부 변경은 관련 작업과 조율하고, 서로 의존하는 변경은 순서대로 통합한다. 새 영속 작업이나 외부 메시지를 사용자 요청 없이 만들지 않는다.

## 승인과 안전

- 승인된 범위의 되돌릴 수 있는 구현·검증·커밋·PR·머지·배포는 필요한 게이트를 통과하면 자율적으로 끝낸다. 이미 받은 허락을 다시 묻지 않는다.
- 별도 사용자 판단이 필요한 경계는 수강생 실데이터의 비가역 변경, 돈·보안(과금·자격증명·방화벽·새 원격접속 경로), 수강생 정책·스펙 방향 전환, 외부 발행(공지·문안)이다. 기존 승인이 해당 범위를 포함하는지 먼저 확인한다.
- 계정·키·토큰·쿠키·비밀번호·연결 문자열·수강생 실데이터를 로그·문서·스크린샷·커밋에 노출하지 않는다. 설정 확인에는 변수명과 설정 여부만 남긴다.
- `raw/`, 외부 vault, 사용자 데이터와 타인의 작업을 임의로 수정·삭제하지 않는다. `HANDOFF.md`, `scratchpad/`, `.env*`, `logs/`도 요청 없이 정리·삭제·커밋하지 않는다.
- 훅·린터·구조 테스트를 우회하거나 통과 목적만으로 테스트를 삭제·약화하지 않는다. `.git/` 내부를 직접 조작하지 않는다.

## 구현과 검증

- 현재 `.githooks/pre-commit`은 main/master 직접 커밋을 막고, `lib/`, `app/`, `components/`, `tests/` 변경 시 `docs/plans/active/`의 활성 계획을 요구한다. 해당 코드 작업에는 짧은 계획을 유지하고 완료 후 `completed/`로 옮긴다. 지침 정리가 이 훅을 해제하지 않는다.
- 코드 변경의 기본 게이트는 `bash scripts/check.sh`다. 런타임·앱 코드가 바뀌면 `npx next build`도 수행한다. 현재 검사 항목과 제한값은 스크립트를 따른다.
- 문서만 수정할 때 검사만을 위해 새 worktree마다 의존성을 설치하지 않는다. 사용할 수 있는 검사와 CI를 확인하고, 실행하지 못한 검사는 이유와 함께 `NOT_RUN`으로 기록한다. 커밋 훅이 실행하는 검사를 우회하지 않는다.
- UI 변경은 실제 화면과 핵심 사용 흐름을 브라우저에서 확인한다. 모바일 입력과 PC 대시보드 사용을 각각 확인하고 필요한 화면 증거를 남긴다. 테스트 통과를 화면 확인으로 대신하지 않는다.
- 새 컴포넌트·타입·시트 키·디자인 토큰은 `CLAUDE.md`가 가리키는 관련 문서와 같은 PR에서 맞춘다. 상세 계약 변경은 기존 ADR 절차를 따른다.
- 검수·테스트·배포를 실제로 수행한 범위만 보고한다. `NOT_RUN`, 실패, 부분 완료를 통과나 완료로 바꾸지 않는다.

## 기록과 협업

- 진행은 현재 작업에 연결된 이슈·PR과 `docs/worklog.md`의 관련 기록을 확인한다. 오래된 Linear BBE-73·BBE-75 번호를 새 게시 의무로 적용하지 않는다. 실제 운영 중인 배정·모니터 연동은 확인 후 유지하며, 확인되지 않은 체계를 임의로 만들지 않는다.
- 기존 배정·운영 작업에는 해당 카드의 범위·상태·완료 증거를 유지한다. 경영일지와 모아워크를 명확히 구분하고, 동일 내용을 여러 문서로 복제하지 않는다. 외부 기록을 쓸 권한·도구가 없으면 로컬 worklog 또는 완료 보고에 남기고 미게시를 명시한다.
- 일반 질문·로컬 설정 정비 때문에 정형 신원 선언, 하트비트 게시, 역할별 문서 전체 읽기를 새로 요구하지 않는다. 실제 운영 중인 모니터나 배정은 변경 요청 없이 삭제하지 않는다.
- 진행 공유는 지금 확인한 결과, 다음 행동, 필요한 사용자 판단 중심으로 한다. 중요한 결정·머지·실패는 기존 작업 기록에 간결하게 남기며 과거 기록은 보존한다.

## 머지·배포·정리

- 머지부터 배포·health 확인까지 한 트랙씩 진행한다. 앞선 변경이 합쳐지면 최신 master와 통합하고 영향에 맞는 검사를 다시 수행한다.
- feat/fix의 squash 커밋 본문에는 수강생이 이해하는 `Changelog:` 한 문장을 넣는다. 묶음 기능·NEW 표시가 필요하면 기존 `Changelog-Group`, `Changelog-Done`, `Changelog-Anchor` 계약을 확인한다.
- 배포 절차는 `.github/workflows/deploy.yml`과 `docs/playbooks/deploy-vps.md`를 확인한다. 머지 전 정상 SHA를 기록하고 배포 결과, 공개 health, 변경한 사용자 흐름까지 확인한다. SSH 연결 실패와 앱 build/health 실패를 구분한다.
- 앱 장애는 정상 버전으로 복구하고 복구 배포도 확인한다. 기본 복구는 문제 커밋의 revert이며 master force-push로 이력을 지우지 않는다. 단순 SSH 실패에 코드 롤백을 적용하지 않는다.
- DB 마이그레이션 파일 추가를 운영 적용 완료로 보고하지 않는다. 실행 경로·승인·결과를 따로 확인한다. 로컬에 운영 비밀값이 없다는 이유로 값을 새로 만들거나 교체하지 않는다.
- 마친 자신의 임시 프로세스·worktree는 미커밋 변경과 사용 여부를 확인한 뒤 정리한다. 다른 세션의 작업은 보존한다. Windows 삭제·이동은 절대 경로를 확인하고 한 셸의 안전한 파일 명령으로 수행한다.

2026-09-09 정비: 반복 역할 선언·낡은 절대 경로·중복 사고 절차를 제거하고, 제품 불변조건과 실제 훅·배포·데이터 보호 경계는 유지했다.

<!-- BEGIN MUSE-CONTRIBUTOR-XHIGH-20260915 -->
## Muse 하위 작업 기본 모델 — 2026-09-15 사용자 승인

- 사용자 승인 범위: 공용 Muse 실행기를 사용하는 후속 위임 및 세일즈PT·경영일지·모아워크. 기본 모델은 `muse-spark-1.3-contributor`, 추론 수준은 `xhigh`다.
- Contributor에 전달한 비민감 입력·응답의 Meta 모델 학습 사용에 사용자가 동의했다. 이 승인은 비밀값·인증정보·수강생 개인정보·무관한 비공개 자료의 전송 승인이 아니다. 기존 최소 자료·비민감 문맥 제한을 유지한다.
- 공식 Muse Code 브라우저 로그인 구독 경로와 `C:/Users/Belief-desktop/.agents/tools/muse_subscription.py`를 사용한다. 별도 API 과금이나 API 키 경로로 전환하지 않는다.
- 파일 쓰기·셸·웹 도구·외부 개인 문맥 비활성화와 최대 모델 단계 4를 유지한다. 이 명시적 요청은 과거 Contributor 임의 전환 금지 규칙에 대한 승인된 예외다.
- 모델과 추론 설정은 실행기 인자 및 제공자 실행 메타데이터로 확인한다. 이미 실행 중인 요청이나 다른 원격 실행 환경까지 변경됐다고 가정하지 않는다.
<!-- END MUSE-CONTRIBUTOR-XHIGH-20260915 -->
