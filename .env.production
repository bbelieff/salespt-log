# 프로덕션 빌드 공개 env — NEXT_PUBLIC_* (클라 번들에 인라인됨) 만 둔다.
# ⚠️ 시크릿(SA 키·토큰·세션 비밀 등) 절대 금지 — 시크릿은 VPS 의 .env.local(gitignore)에.
# next build 가 .env.production 을 로드해 빌드 시점에 값 인라인.
NEXT_PUBLIC_GUIDE_URL=https://climbing-caraway-ec3.notion.site/3813fa7fca0080279c0be4438df786aa
