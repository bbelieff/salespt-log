logs/agent/*
!logs/agent/.gitkeep
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts
.DS_Store

# secrets — NEVER commit
*.json
!package.json
!package-lock.json
!tsconfig.json
!.eslintrc.json
!components.json
.env
.env.*
!.env.example


# bkit tooling state — never commit
.bkit/
backups/
졸업생총회_발표자료/

# 1회성 마이그레이션 백업(PII — 커밋 금지)
scripts/_backup-*.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# 클로드코드 워크트리는 .claude/ 아래에 산다. graft 가 .claude/settings.json 을
# 커밋 대상으로 만들기 때문에, 워크트리까지 딸려 들어가지 않도록 여기서 막는다.
/.claude/worktrees/
