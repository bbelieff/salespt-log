# 하단 STEP 바 반투명 목업 — 2026-09-14

## 최신 정정: 연결 토글 + 개선 원형
- 역할은 개별 알약이 아닌 하나의 연결 트랙(26px 시각높이) 안에서 선택되는 세그먼트 토글. 버튼44px, 기존 busy/대리접속/aria-pressed/가드 유지. Muse 구현안을 통합하되 기능조건 변경 제안은 제외했다.
- 캘린더는44×44px 유리 원형, 곡률50%, translateY(-8px), 흰색45% 표면/60% 테두리/가벼운 내부빛. 기존52px 불투명 원과 큰 그림자를 대체. 모바일 약7px 돌출, 라벨 하단18px 유지.
- 320px 실제 렌더링: 가로 넘침 없음, 토글 트랙26px, 바 약83px. 변경 TSX lint 통과. 운영 미배포.
- 아래 과거안 중 개별 역할 알약 및 캘린더 캡슐 결정은 이 정정으로 대체한다.

## 헤더 역할 토글 일관성
- 대시보드/수강생/트레이너는 공통 `.app-header-pill` 사용: 둥근 전체 곡률, red200 테두리, red50 배경, red700 글씨. 선택 역할만 red700 배경/흰색 글씨.
- 모바일 시각 높이26px, 실제 역할 버튼 터치높이44px. 좌우 여백은 모바일8px/넓은화면12px. 320px 뷰포트에서 가로 넘침 없이 동일 높이 한 줄 확인.
- 기존 권한·대리접속·aria-pressed·disabled·미저장 가드와 전환 API 동작은 유지. 변경 TSX lint 오류 없음(기존 TopHeader 경고2개 유지), diff 검사 통과. 로컬 적용·운영 미배포.

## 2차 수정 (현재)
- 바 높이 96→83px, 본문 예약 공간 108→83px. 일반 탭 상단 패딩 제거, 간격 4→2px. 글꼴과 라벨 바닥 여백 18px 유지.
- white 32% + blur 20px + saturate 180%, 약한 그림자, 상단 곡률16px.
- 320px 화면 및 비교 프레임 실측: 바83px, 본문padding83px, 다섯 라벨 하단 동일, 가로 넘침 없음. 변경 TSX ESLint 통과. 운영 미배포.
- Muse 실제 제안 성공(62.809초). 제안 중 불투명도 증가와 행 높이 계산은 총괄이 사용자 요구와 실제 콘텐츠 크기에 맞춰 보정.

아래는 1차 목업 기록이다.

- 요청: 하단 글씨 잘림 방지를 위한 소폭 상향, iPhone 느낌의 반투명 배경, 목업 확인.
- TabBar 순서·아이콘·단계색·미저장 가드는 유지. 캘린더 라벨과 일반 탭 하단 패딩을 통일.
- 최소 안전여백 8px+추가 4px, 반투명 white 78%/blur 22px/saturate 180%, 상단 22px 라운드. 블러 미지원/투명도 감소 설정은 불투명 배경.
- 본문 예약 높이도 공통 토큰을 사용. Chrome 320/430px 뷰포트에서 가로 넘침 없음, 다섯 라벨 하단 좌표 일치 및 화면 끝까지 18px 여백 확인. 390px 외곽 프레임 비교 목업도 화면 확인.
- TypeScript 및 변경 TSX 파일 ESLint 통과. 실제 iPhone Safari와 34px 안전영역 기기 검증은 미실시.
- 비교 목업: http://127.0.0.1:54704/compare (로컬 서버 실행 중에 열림).
- 실제 컴포넌트 목업 단계. 운영 배포 완료를 뜻하지 않음.
