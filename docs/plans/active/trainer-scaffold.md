# Trainer management scaffold

UI-only reorganization requested by user. Preserve queries, mutation handlers, lists and permissions.
- Three peer groups: trainer management(count), student roster(count), management staff(count).
- Trainer group contains assignment, permissions, requests, invitations, with consistent disclosure cards.
- Remove redundant guidance and duplicate editor title; keep errors, busy/unsaved status and confirmations.
- Verify responsive layout, existing regressions, full gate/build, deployment/live screen.
