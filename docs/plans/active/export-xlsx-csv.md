---
slug: export-xlsx-csv
status: draft
created: 2026-08-09
owner: 데탑 C작업원C(260809) — belie 지시("여유 되면 설계부터")
related: sheet-retirement-r7(#22), db-write-flip
---

> **📄 이 문서는 무엇인가요?**
> - **한 줄 요약**: 시트 은퇴(R7) 후에도 학생·운영진이 자기 데이터를 파일로 가져갈 수 있게 하는
>   "Export(xlsx/csv) 버튼" — BBE-71 — 의 첫 설계 초안. 아직 구현 착수 아님.
> - **누가 읽나요**: belie(형식·범위·주체 결정), 실행 워커(착수 시점 배차), 반장(레인·머지 판정)
> - **어떤 기능·작업과 연결?**: `docs/plans/active/sheet-retirement-r7.md` Phase 4 #22
>   (BBE-71) — `#20`(시트 미러 폐기, BBE-69 추정)의 **선행**. 없으면 시트를 못 끈다.
> - **읽고 나면 알 수 있는 것**: 왜 지금 코드가 아니라 설계만 하는지 / 무엇이 미결이라 belie
>   결정이 필요한지 / 구현 시작 시 어떤 모양이 될지(레이어·라이브러리·API)
> - **관련 문서**: `sheet-retirement-r7.md` §D·§G.1·Phase 4

## 0. 왜 지금은 설계만인가

belie 지시("BBE-71 착수... 여유 생기면 설계라도 미리 시작해라")와 R7 로드맵의 배치가 일치한다 —
Phase 4 표에서 #22(이 카드)의 **선행 = #19(파일럿 게이트 제거)**. #19 는 Phase 1(레지스트리
DB화, 아직 진행 중 — BBE-56 은 오늘 게이트 OFF 배선까지만 완료)과 Phase 2(append 재키잉 등)가
전부 끝나야 착수 가능한 **R7 임계경로의 끝단**이다. 지금 구현하면:
- 데이터 소스가 아직 시트/DB 혼재라 export 결과가 어느 시점 기준인지 불명확해진다.
- #19 전엔 "게이트 하나로 전 화면 롤백" 안전망이 살아있는데, export 를 미리 만들면 그 배선이
  또 하나의 롤백 예외 지점이 된다(§5 리스크 원칙과 충돌).

그래서 이 문서는 **구현 없이** 다음만 한다: R7 §G.1 이 이미 적어둔 3가지 미결(형식·범위·주체)에
대한 권장안 제시, 기술 설계 초안(레이어·라이브러리 후보), 수용 기준 초안. belie 결정이 나면
`status: active`로 승격하고 실행 계약(task_id·file lease)을 붙인다.

## 1. 미결 3가지 (R7 §G.1) — 권장안

### ① 형식 — xlsx 단일로 시작, csv 는 보류(YAGNI)
카드 제목이 "xlsx/csv" 둘 다 언급하지만, 실사용 맥락(학생이 잃는 것 = **여러 탭짜리 구글시트**)을
보면 xlsx(다중 시트 워크북)가 지금 경험과 1:1 대응이라 더 낫다. csv 는 탭 하나만 담을 수 있어
"내 기록 전체"를 표현 못 한다(탭마다 파일이 여러 개로 쪼개짐). **CLAUDE.md YAGNI 원칙**("언젠가
쓸 수도 있어서" 금지) 대로, 구체적 필요(예: 다른 프로그램에 기계적으로 재수입)가 실제로 나오기
전까지는 xlsx 하나만 만든다. csv 는 후속 카드로 미룬다.
- **belie 결정 필요 여부**: 낮음(권장안 채택 시 질문 불요) — 반대 의견 있으면 알려달라.

### ② 범위 — 개인 전체 스냅샷(현재 탭 구조를 시트별로 그대로)
"시트는 다운로드 버튼 형태로 남긴다"(belie 08-05 확정)의 취지 = 오늘 시트가 담고 있는 걸
그대로 파일로 가져갈 수 있으면 된다. 그러므로 MVP 범위 = **로그인한 사용자 1명의 데이터 전체**를
현재 탭 구조 그대로 시트 여러 개(워크시트)로 묶는다:
| 워크북 시트 | 소스(오늘 기준) | 비고 |
|---|---|---|
| 업체관리(04) | `meetings` | 미팅 전체 |
| 계약수납(02) | `contracts` | 계약 전체(BBE-53 자연키 upsert 이후 안전) |
| DB관리(03) | `sales` | 4섹션 |
| 영업관리 | `sales`+`meetings` 재계산 | I~P 는 앱이 이미 계산하는 L·N 만(§C, 나머지는 폐기값) |
| 대시보드 요약 | `dashboard-aggregates.ts` 함수 재사용 | 전광판 5지표는 DB 경로 없어 이 카드가 첫 DB화가 될 수도(§4 참고) |

**belie 결정 필요**: **범위를 "학생 개인 전체"로 한정**하는 것에 동의하는지. (기수 전체 admin
일괄 export 는 다른 유스케이스라 별도 카드 권장 — 아래 ③ 참조)

### ③ 사용 주체 — 학생 self-service 우선, admin 대량은 후속
로그인한 학생이 "내 기록 내려받기" 버튼으로 자기 데이터만 받는 것이 1순위(시트 대체 목적에
정확히 부합). admin/트레이너/회장의 **여러 학생 일괄** export 는 스코프가 다르다(권한 검사·
대용량 처리·zip 묶음 등 추가 설계 필요) — 이 카드에서 안 하고 후속 카드로 분리 제안.
- **belie 결정 필요**: 위 우선순위(학생 우선, admin 일괄은 후속 카드)에 동의하는지.

## 2. 기술 설계 초안

### 레이어 (CLAUDE.md §2 단방향 원칙 준수)
```
lib/service/export-xlsx.ts   — 순수 조립: 각 도메인 서비스 함수를 호출해 워크북 데이터 구조로 변환
                                (다른 service 함수를 호출하는 것은 기존 관행, contract-payment-add.ts 참고)
app/api/export/route.ts      — Route Handler: 인증(본인 이메일만) → service 호출 → xlsx 버퍼 반환
                                Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
                                Content-Disposition: attachment; filename="내기록_{날짜}.xlsx"
components/                  — "내 기록 다운로드" 버튼(설정 화면 후보) — 클릭 시 위 라우트로 이동/fetch
```
`googleapis` 는 이 경로 어디에도 안 들어간다(§2 규칙2 저촉 없음) — xlsx 생성은 Sheets I/O 가
아니라 순수 데이터 직렬화다.

### 라이브러리 후보
현재 xlsx/csv 의존성 0(R7 §D 확정). 후보 2개:
| 후보 | 장점 | 단점 |
|---|---|---|
| **`xlsx`(SheetJS CE)** | 가볍다(VPS 빌드 메모리 제약 — `NODE_OPTIONS=--max-old-space-size=2048`
  참고), 멀티시트 쓰기 무료(CE)로 충분, API 단순 | 셀 스타일링 제한적(지금 요구사항엔 불필요) |
| `exceljs` | 서식·freeze pane 등 풍부 | 더 무겁다, 지금 필요 없는 기능 |

**권장 = `xlsx`(SheetJS)**. 지금 요구(데이터 그대로 옮기기)에 서식 기능이 불필요하고, "Tools
적을수록 좋다"(CLAUDE.md §0) 원칙과도 맞는다. 서식 요구가 나중에 생기면 그때 재검토.

### 데이터 소스 — DB 우선, 시트 폴백은 없음(§0.7 비파일럿은 아직 대상 아님)
이 기능 자체가 R7 Phase 4(#19 이후) 소관이라, 착수 시점엔 전 기수가 DB 정본일 가능성이 높다.
혹시 그 전에 당겨서 만들게 되면(예: belie 가 우선순위를 바꾸면) 각 소스 함수가 이미 갖고 있는
`dbEnabled()+isDbReadPilot` 게이트를 그대로 재사용 — 새 판정 기준을 만들지 않는다(기존 R2/R3
관행).

## 3. 수용 기준 (초안 — belie 결정 후 확정)

- [ ] belie: ①형식 ②범위 ③주체 결정(위 §1) — 권장안 그대로면 "승인"만으로 충분
- [ ] 로그인 사용자가 "내 기록 다운로드" 클릭 → xlsx 1개 파일(5개 워크시트) 다운로드
- [ ] 워크시트별 헤더는 화면 표시 라벨과 동일(한글, 시트 원본 컬럼명 아님 — 사람이 열어보는
      용도라 SHEET_RANGES 내부 코드가 아니라 UI 라벨 기준)
- [ ] 본인 데이터만 접근 가능(인증 미들웨어 재사용, 타 사용자 이메일 파라미터 주입 차단 테스트)
- [ ] 대용량(수백 행) 워크북 생성이 API 타임아웃 안에 끝남(VPS 자원 확인)
- [ ] check.sh 초록 (구현 단계에서)
- [ ] §6.8 배포 확인

## 4. 남은 위험 / 후속 결정 필요

- **전광판 주차별 5지표(C33:H40) = DB 경로가 아직 없다**(R7 §C 확정) — 이 카드가 그 경로를
  처음 만들게 될 수도 있다. 범위에 넣을지, "대시보드 요약은 후속"으로 미룰지는 belie 결정
  또는 착수 시점 반장 판단.
- admin/트레이너/회장의 **일괄 export**는 이 카드 범위 밖(§1③) — 후속 카드로 별도 발행 권장.
- B21 누적수임비 정의는 BBE-66 으로 확정됨(대시보드 요약 시트에 포함 시 그 정의 재사용).

## Log
- 2026-08-09 착수(데탑 C작업원C(260809)): belie 직접 지시 — BBE-64 완주 후 "여유 되면 설계부터".
  R7 로드맵(#22, 선행=#19) 확인 → 지금은 구현 착수 시점이 아님을 실측으로 확인 → 설계 초안만
  작성. §G.1 미결 3가지에 권장안 제시, worklog 결정함에 belie 질문 등재(쉬운 말, §0.6 형식).
