---
slug: google-calendar-sync
status: active
created: 2026-07-06
owner: belie
related: sheet-structure, components, 0015-admin-oauth-drive-create, consultation-log-and-calendar
---

> **📄 이 문서는 무엇인가요?**
> - **한 줄 요약**: 앱의 일정(미팅 예약·일반이벤트·실무투두 예정일)을 수강생 본인의 구글 캘린더에 단방향(앱→구글) 자동 동기화하는 기능의 설계 기획서.
> - **누가 읽나요**: 개발자, 에이전트, 운영자(belie)
> - **어떤 기능·작업과 연결?**: 04 업체관리(미팅)·05 실무투두(일반이벤트/투두), 캘린더 탭, NextAuth(Google), 레지스트리(토큰 저장), GCP OAuth 클라이언트
> - **읽고 나면 알 수 있는 것**: 무엇이 어느 캘린더로 가나, 인증·토큰을 어디 두나, 수정/삭제가 어떻게 전파되나, PR을 어떻게 쪼개나
> - **관련 문서**: docs/domains/sheet-structure.md, ADR-0015, docs/playbooks/live-canary.md

# 구글 캘린더 동기화 (앱 → 구글, 단방향)

## 0. 확정 결정 (2026-07-06 사용자)
- **방향**: 단방향 앱→구글. 구글에서의 수정은 앱에 안 돌아옴(SSOT=시트 원칙 유지). 양방향(webhook) 기각 — 복잡도 대비 가치 낮음(YAGNI).
- **대상**: ① 미팅 예약(04) ② 일반이벤트(05, 기존/기타) ③ 실무투두(05) 예정일 있는 항목 — 전부.
- **전파**: 앱에서 수정(날짜·시각·업체명 등)·삭제 시 구글 이벤트도 patch/delete. 미팅 완료는 과거 이벤트로 유지.
- **연결 단위**: 수강생 본인 구글 계정. opt-in(연결 버튼 누른 사람만). **대상 캘린더는 사용자가 선택**(기본=primary, 연동 카드 드롭다운 — 2026-07-06 추가 확정). 전용 캘린더 *생성*은 후속.
- **대상 선택 방식 (2026-07-09 변경)**: 유형 토글 3종(미팅/실무/일반) **폐기** → **일정별 개별 토글(기본 ON)**. 연결하면 모든 새 일정이 자동 등록되고, 특정 일정만 캘린더 탭에서 개별로 뺀다. gcal_settings 는 calendarId 만 저장(토글 상태 저장 없음), 일정별 on/off 는 `gcal_event_ids` 마커로 gcal-2 가 관리(§2). 이유: "미팅 전체 on/off"보다 "이 일정만 빼기"가 실제 사용 패턴에 맞음.

## 1. 인증 — 수강생 개인 OAuth (ADR-0028 신규 작성)
- 로그인(NextAuth) 스코프는 **무변경**. 캘린더 탭의 [구글 캘린더 연결] 버튼 → 별도 OAuth 동의
  (scope 2개: `calendar.events` + `calendar.readonly` — 후자는 캘린더 목록 드롭다운용 읽기 전용.
  calendar.events 만으로는 calendarList 조회 불가) → per-user refresh token 획득.
  ADR-0015(관리자 Drive 토큰)와 같은 패턴의 사용자판.
- **토큰 저장**: 레지스트리 users 탭 신규 컬럼 `gcal_token` — AES-256-GCM 암호화(AUTH_SECRET 파생키),
  평문 저장 절대 금지. + `gcal_settings`(JSON: `{calendarId}` — 대상 캘린더만. 유형 토글 폐기 2026-07-09).
  컬럼은 끝에 append(shift 금지), sheet-structure.md §6 등재.
- 연결 해제 = 토큰 컬럼 비움 + (가능하면) 구글 token revoke. 기존 구글 이벤트는 남김(안내 문구).
- ⚠️ **운영 리스크**: `calendar.events` 는 Google 기준 sensitive scope — 앱이 브랜드 검증 전이면
  동의 화면에 "확인되지 않은 앱" 경고 또는 테스트 사용자(100명) 제한. 단기: GCP 동의화면
  테스트 모드에서 수강생 이메일을 테스트 사용자로 등록(현 인원 ~40명, 여유) 또는 경고 안내 문구.
  장기: 브랜드 검증 신청. 사전 GCP 작업은 §5.

## 2. 데이터 — 이벤트 매핑·멱등
- **04 미팅**: 신규 컬럼 `gcal_event_ids` 1개 append(현재 마지막 컬럼 뒤 — 구현 시 실제 끝 컬럼
  git/시트에서 확인). **05 실무투두**: 동일하게 append. sheet-structure.md 등재.
  ★값 = **사용자별 JSON 맵** `{"이메일": "eventId" | "-"}` — `eventId`=등록됨, `"-"`=사용자가
  일정별 토글로 뺌(**제외 마커**), **키 없음=기본 ON**. 한 시트 멀티 계정(부부·직원)이 각자
  연결해도 충돌 없이 각자 캘린더에 등록·제외(2026-07-06 QA 에서 발견된 설계 구멍 보완).
- 멱등 규칙(사용자 단위): 내 이메일 키에 id·마커 없으면(기본 ON) insert 후 저장, id 있으면 patch,
  삭제 시 delete 후 내 키만 제거. **제외 마커 `"-"` 는 수정 훅·[다시 올리기]가 절대 되살리지 않음**
  (일정별 토글 OFF→ON 복귀만 재 insert). §2.5 bulk-write 보존 가드 준수.
- 이벤트 내용(비-PII 최소) — **제목은 업체명/할일이 맨 앞**(모바일 잘림 방지, 2026-07-06 확정.
  `[세일즈PT]` prefix 금지 — 출처 식별은 제목이 아니라 extendedProperties 로):
  - 미팅: 제목 `{업체명} 미팅`, 시작=예약일+예약시각(기본 1시간), 장소=H, 설명=앱 링크 + "세일즈PT 경영일지".
  - 일반이벤트: 제목 `{이벤트명}`, 카테고리(기존/기타)·출처는 설명에.
  - 투두: 예정일자만 있으면 **종일 이벤트**, 예정시각 있으면 시각 이벤트. 제목 `{할 일 제목}`.
  - 앱 생성 이벤트 식별: `extendedProperties.private.salesptId = {행 id}` — 제목과 무관하게 추적·멱등.
- **대상 캘린더 선택**: calendarList.list 로 쓰기 가능 캘린더 드롭다운(기본 primary),
  gcal_settings 에 calendarId 저장. 캘린더 변경 시 신규 이벤트는 새 캘린더로, 기존 이벤트는
  [전체 다시 동기화] 실행 시 events.move 로 이동(안내 문구 표시).
- 이월 미팅도 예약 상태면 동기화 대상(수강생 실제 일정이므로). 아레나 집계와 무관.

## 3. 동기화 엔진
- `lib/repo/gcal-client.ts` — googleapis calendar v3 (googleapis 는 lib/repo/ 전용 규칙 그대로).
  `lib/service/gcal-sync.ts` — 매핑·멱등·설정 판정 단일 진입점.
- **훅 지점(service 레이어)**: 미팅 생성/수정/삭제·상태 변경, 일반이벤트/투두 생성/수정/삭제 —
  각 서비스 유스케이스 끝에서 fire-and-forget 호출.
- **베스트에포트·비차단**: 구글 API 실패해도 앱 저장은 성공(경고 로그·PostHog 이벤트). 재시도 1회.
  연결 안 한 사용자·개별 토글로 뺀 일정(제외 마커 `"-"`)은 즉시 skip(비용 0).
- [전체 다시 동기화] 버튼: 현재 시트의 대상 항목 전체를 멱등 push(실패 복구용). 페이지네이션·quota
  주의(사용자당 이벤트 수십 개 수준이라 여유).
- 토큰 만료/철회 감지 → gcal_token 무효 표시 + 설정 카드에 "다시 연결해 주세요" 상태.

## 4. UI — 캘린더 탭 상단 "구글 캘린더 연동" 카드
### 4-1. 확정 문구 (2026-07-06 — 모바일 360px 1줄 원칙, 잘림(…)·의도치 않은 줄바꿈 금지)
| 요소 | 확정 문구 | 비고 |
|---|---|---|
| 카드 제목 | 구글 캘린더 연동 | |
| 미연결 설명 | 예약한 일정이 내 구글 캘린더에 자동으로 등록돼요 | 명시적 2줄 개행 허용 |
| 연결 버튼 | 연결하기 | |
| 상태 뱃지 | 연결됨 / 연결이 풀렸어요 | "만료" 금지 |
| 재연결 버튼 | 다시 연결 | |
| 캘린더 선택 라벨 | 캘린더 | 캘린더 이름만 말줄임 허용(사용자 데이터) |
| 재동기화 버튼 | 다시 올리기 | "동기화" 용어는 사용자 화면에서 금지. 버튼은 gcal-2(엔진 필요) |
| 일정별 토글 토스트 | 구글 캘린더에 담았어요 / 구글 캘린더에서 뺐어요 | 일정별 개별 토글(gcal-2). "동기화" 금지 |
| 해제 버튼 | 연결 해제 | |
| 해제 안내 | 이미 등록된 일정은 구글에 남아요 | |
| 캘린더 변경 토스트 | 새 일정부터 이 캘린더에 담겨요 | 기존분 이동은 [다시 올리기] 안내 |
- 금지 용어(사용자 화면): 동기화, 토큰, 만료, OAuth, 캘린더 ID. 개발 문서·로그에서만 사용.
- 미연결: [연결하기] 버튼 + 설명 1개. **연결됨(gcal-1 현행)**: 캘린더 드롭다운(즉시 저장) · [연결 해제].
  실패 상태: "연결이 풀렸어요 — 다시 연결" 배너. 토스 문체, 기존 컴포넌트 재사용, components.md 등재.
- **gcal-2b 구현됨**: 계정 표시(카드) · [다시 올리기](카드, 전체 재푸시) · **일정별 개별 토글**
  (GcalItemToggle — 캘린더 탭 각 일정 항목 우측, 카드 아님). 유형 토글 3종은 폐기(2026-07-09).

## 5. 사전 GCP 작업 (운영자 belie — 코드 밖)
1. GCP 프로젝트(saleslog-494703)에서 **Google Calendar API 사용 설정**.
2. OAuth 동의화면에 scope `calendar.events` 추가, (테스트 모드면) 수강생 이메일 테스트 사용자 등록.
3. 기존 OAuth 클라이언트(salesptlog-prod)에 리디렉션 URI 추가:
   `https://salesptlog.online/api/gcal/callback`, `http://localhost:3000/api/gcal/callback`.

## 6. 구현 분할 (PR)
1. **docs/gcal-sync-plan** — 본 문서 + ADR-0028(사용자별 캘린더 토큰·시트 암호화 저장) 등재.
2. **feat/gcal-connect** — OAuth 연결/해제 flow + 토큰 암호화 저장 + 연동 카드(캘린더 드롭다운·해제).
   (유형 토글 3종 폐기 2026-07-09 → 일정별 개별 토글은 3번으로. #511 후 feat/gcal-drop-type-toggles 로 개정.)
3. **feat/gcal-sync-engine (gcal-2a)** — gcal-client(calendar v3)/gcal-sync(멱등) + gcal_event_ids
   컬럼(meetings AT·todos O) + 생성/수정/삭제 전파 훅. 연결 시 모든 새 일정 **기본 ON 자동 등록**.
   (2 머지 후. 위험구간=미팅 시트 스키마 확장 → 여기서 격리·검증)
4. **feat/gcal-toggle-resync (gcal-2b)** — **일정별 개별 토글(기본 ON, 제외 마커 `"-"`)** + [다시 올리기]
   + 계정 표시. 구현: gcal-sync(toggleSchedule/resyncAll) · gcal-event-ids(readGcalStates 배치) ·
   gcal-schedule-read(전체 열거) · /api/gcal/{toggle,resync,states} · GcalItemToggle · 카드 개정.
   (3a 머지 후. Changelog-Done 으로 gcal 그룹 공개)
- 테스트: 단위(암복호화·매핑·멱등 판정) + 구조(googleapis 격리에 gcal-client 포함) +
  라이브 카나리아(연결→미팅 생성→구글 확인→시간 수정→삭제, live-canary 플레이북).

## 7. 스코프 밖 (후속·기각)
- 양방향 동기화(webhook/watch) — 기각(YAGNI). 전용 "세일즈PT" 캘린더 생성 — 후속(스코프 확대).
- 트레이너/관리자 캘린더, 리마인더 알림 커스텀 — 후속.

## 8. QA 체크리스트 (경우의 수 전수 — 구현 PR 수용 기준의 일부)
### 8-1. 연결 상태 × 동작
| # | 케이스 | 기대 결과 |
|---|---|---|
| 1 | 미연결 사용자가 미팅 생성 | 구글 호출 0회(즉시 skip), 앱 정상 |
| 2 | 연결 상태에서 미팅 생성(기본 ON) | 선택 캘린더에 "{업체명} 미팅" 등록, id 저장 |
| 3 | 등록된 일정을 개별 토글 OFF | 구글에서 delete + 제외 마커 `"-"` 저장, "구글 캘린더에서 뺐어요" |
| 4 | 개별 토글 OFF → 다시 ON | 재 insert(새 eventId), "구글 캘린더에 담았어요". 마커는 [다시 올리기]가 안 되살림 |
| 4b | 미연결 사용자가 일정 토글 조작 | "먼저 구글 캘린더를 연결해 주세요" + 연결 유도, 구글 호출 0회 |
| 5 | 연결 해제 후 미팅 생성 | 등록 안 됨, 기존 구글 일정은 남음 |
| 6 | 토큰 철회(구글 계정에서 앱 액세스 삭제) 후 미팅 생성 | 앱 저장 정상 + 카드 "연결이 풀렸어요" 전환 |
### 8-2. 대상별 생성/수정/삭제 (연결 전제, 해당 일정 기본 ON)
| # | 케이스 | 기대 결과 |
|---|---|---|
| 7 | 미팅 날짜/시각 변경 | 구글 이벤트 이동(patch) |
| 8 | 미팅 업체명 변경 | 제목 갱신 |
| 9 | 미팅 취소 | 구글 이벤트 삭제, id 제거 |
| 10 | 미팅 삭제(행 삭제·cascade 포함) | 구글 이벤트 삭제 |
| 11 | 미팅 완료 처리 | 구글 이벤트 유지(과거 일정) |
| 12 | 이월 미팅(구분=이월, 상태=예약) | 등록됨(실제 일정) — 집계와 무관 |
| 13 | 일반이벤트 생성/수정/삭제 (기존·기타 각각) | 제목 "{이벤트명}" 등록/갱신/삭제 |
| 14 | 실무투두 예정일만(시각 없음) | 종일 이벤트 |
| 15 | 실무투두 예정일+시각 | 시각 이벤트(기본 1시간) |
| 16 | 실무투두 예정일 없음 | 등록 안 됨(대상 아님) |
| 17 | 실무투두 완료 체크 | 구글 이벤트 유지 |
### 8-3. 캘린더 선택·멀티 계정·복구
| # | 케이스 | 기대 결과 |
|---|---|---|
| 18 | 캘린더 A→B 변경 후 새 미팅 | B에 등록. 기존 A 일정은 그대로 + 토스트 안내 |
| 19 | 캘린더 변경 후 [다시 올리기] | 기존 이벤트 events.move 로 B 이동, 중복 생성 0 |
| 20 | [다시 올리기] 연속 2회 | 멱등 — 중복 이벤트 0 |
| 21 | 한 시트 2계정(부부) 각자 연결 | 각자 자기 캘린더에 등록, gcal_event_ids JSON 에 키 2개 |
| 22 | 2계정 중 1명이 미팅 수정 | 두 사람 캘린더 모두 patch(각 키의 id 로) |
| 23 | 구글 API 일시 실패(500/네트워크) | 앱 저장 성공, 재시도 1회, 실패 로그 — 이후 [다시 올리기]로 복구 |
| 24 | 같은 미팅 빠른 연속 저장(더블클릭) | 이벤트 1개만(id 저장 후 patch 경로) |
| 25 | 쓰기 권한 없는 캘린더(구독 캘린더) | 드롭다운에서 제외(쓰기 가능만 노출) |
### 8-4. 문구·모바일 레이아웃 (360px 기준)
| # | 체크 | 기준 |
|---|---|---|
| 26 | 연동 카드 모든 고정 문구 | §4-1 확정 문구 그대로, 잘림(…)·의도치 않은 줄바꿈 0건 |
| 27 | 일정별 토글(gcal-2) | 캘린더 탭 각 일정 항목에, 기본 ON 표시, 한 줄 안 깨짐 |
| 28 | 긴 캘린더 이름(20자+) | 이름만 말줄임, 레이아웃 안 깨짐 |
| 29 | 긴 업체명(15자+) 구글 이벤트 | 제목 "{업체명} 미팅" 그대로(자르지 않음 — 구글이 처리) |
| 30 | 금지 용어 스캔 | 사용자 화면에 동기화/토큰/만료/OAuth/ID 0건(테스트로 grep) |

## Log
- 2026-07-06 기획 확정: 단방향·대상 전부·수정/삭제 전파·opt-in 개인 OAuth·시트 암호화 토큰.
- 2026-07-06 QA 확정: 토글 라벨 미팅/실무/일반, 확정 문구표(§4-1)·금지 용어, 멀티 계정
  gcal_event_ids JSON 맵(§2), QA 30케이스(§8)를 구현 PR 수용 기준에 포함.
- 2026-07-06 추가 확정: 제목은 업체명/할일 우선(prefix 금지, 식별=extendedProperties) ·
  대상 캘린더 사용자 선택(calendar.readonly 스코프 추가, 변경 시 재동기화로 events.move).
- 2026-07-09 설계 변경(사용자): **유형 토글 3종(미팅/실무/일반) 폐기 → 일정별 개별 토글(기본 ON)**.
  gcal_settings=calendarId 만, 일정별 on/off=gcal_event_ids 마커(`"-"`=제외). §0·§1·§2·§3·§4·§6·§8 반영.
  gcal-1(#511, 토글 포함본)은 feat/gcal-drop-type-toggles 로 카드·스키마 개정. 개별 토글 UI·엔진은 gcal-2.
