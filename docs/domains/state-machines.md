> **📄 이 문서는 무엇인가요?**
> - **한 줄 요약**: 세일즈PT 영업일지 시스템의 핵심 엔티티 상태 전이 다이어그램
> - **누가 읽나요**: 개발자
> - **어떤 기능·작업과 연결?**: 비즈니스 로직 구현, 상태 관리, API 설계
> - **읽고 나면 알 수 있는 것**:
>   - Meeting, Payment, DailyEntry, DBOrder 각 엔티티의 상태 흐름
>   - 상태 전이를 트리거하는 사용자 액션과 시스템 이벤트
> - **관련 문서**: [ER 다이어그램](./er-diagram.md), [데이터 모델](./data-model.md), [API 명세](./api-spec.md)

# 상태 전이 다이어그램

## 1. Meeting 상태 전이 (5상태) ⭐

> 일정·계약 탭 시안 v2 기준. 시트 J열에 저장되는 상태값은: `예약 | 완료 | 계약 | 변경 | 취소` (5종).

```mermaid
stateDiagram-v2
    [*] --> 예약 : 컨택성공 시 자동 생성\n(업체관리 append, 예약일/시각 기록)

    예약 --> 계약 : 💵 계약 액션\n(수임비 + 계약조건 입력)
    예약 --> 완료 : 🟠 완료 액션\n(미팅 진행, 계약은 X, 사유 필수)
    예약 --> 변경 : 📅 변경 액션\n(새 날짜·시간 입력)
    예약 --> 취소 : 🔴 취소 액션\n(취소 사유 필수)

    계약 --> 예약 : ↶ 되돌리기 (실수 복구)
    완료 --> 예약 : ↶ 되돌리기
    변경 --> 예약 : ↶ 되돌리기
    취소 --> 예약 : ↶ 되돌리기

    변경 --> [*] : 변경 카드는 종결 (무효 + 보존)\n새 카드가 변경된 날에 생성됨

    계약 --> [*] : 종결 (수임비 O열 합산)
    완료 --> [*] : 종결 (M열 사유 누적)
    취소 --> [*] : 종결 (M열 사유 누적)

    note right of 예약 : 초기 상태\n시트 J열 = "예약"\n노란 카드로 표시 (처리 대기 신호)
    note right of 계약 : J="계약", K=true,\nL=수임비, M=계약조건\n→ 영업관리 O,P열 자동
    note right of 완료 : J="완료"\nM열에 "[완료(계약X) · timestamp] 사유" prepend
    note right of 변경 : J="변경"\nM열에 "[일정변경 · timestamp] → 새날짜 새시간"\n새 미팅 행 append (J=예약)
    note right of 취소 : J="취소"\nM열에 "[취소 · timestamp] 사유" prepend
```

### 5상태 비교 표

| 상태 | 시트 J열 | 카드 색 | 다음 액션 가능 | 종결 여부 |
|---|---|---|---|---|
| 예약 (reserved) | `"예약"` | 🟡 노랑 | 4가지 액션 (계약/완료/변경/취소) | ❌ 처리 대기 |
| 계약 (contract) | `"계약"` | 💵 진초록 | 수임비/조건 수정, 예약으로 되돌리기 | ✅ |
| 완료 (done) | `"완료"` | 🟠 주황 | 사유 추가, 예약으로 되돌리기 | ✅ |
| 변경 (rescheduled) | `"변경"` | 📅 보라 | 예약으로 되돌리기 (새 카드는 별도) | ✅ |
| 취소 (canceled) | `"취소"` | 🔴 빨강 | 사유 수정, 예약으로 되돌리기 | ✅ |

### 핵심 규칙
1. **변경**은 단순 상태 전환이 아니라 **새 미팅 행 자동 생성** + 기존 행은 변경 상태로 보존 (이력 추적)
2. 모든 종결 상태 → 예약으로 **되돌리기** 가능 (실수 복구 안전망)
3. **미팅결과(M열)**는 prepend 누적 (최신 위) → 한 미팅의 변경 이력을 시간순 역방향으로 추적

## 2. Payment 상태 전이

```mermaid
stateDiagram-v2
    [*] --> 미승인 : 승인 대기 중인 건 발생
    
    미승인 --> 승인 : 기관 승인 완료 (시스템/사용자)
    미승인 --> 반려 : 승인 거절 (시스템)
    
    승인 --> 수납완료 : 실제 입금 확인 (사용자 확인)
    승인 --> 수납대기 : 입금 지연 상태
    
    수납대기 --> 수납완료 : 지연된 입금 완료
    수납대기 --> 수납취소 : 입금 취소/환불
    
    수납완료 --> [*] : 최종 완료
    반려 --> [*] : 처리 종료
    수납취소 --> [*] : 취소 처리 완료
    
    note right of 미승인 : 수납관리 탭\n승인건수 입력 (C열)
    note right of 승인 : 승인 처리 후\n승인건수 증가
    note right of 수납완료 : 수납건수(D열), 수납금액(E열)\n기관접수내용(F열) 입력
```

## 3. DailyEntry 저장 상태 전이

```mermaid
stateDiagram-v2
    [*] --> draft : 사용자가 데이터 입력 시작
    
    draft --> 검증중 : 저장 버튼 클릭 (사용자 액션)
    
    검증중 --> 검증실패 : 필수값 누락/형식 오류 (시스템 검증)
    검증중 --> 저장중 : 검증 통과 (시스템)
    
    검증실패 --> draft : 사용자가 오류 수정
    
    저장중 --> saved : Google Sheets 저장 성공 (시스템)
    저장중 --> 저장실패 : 네트워크/권한 오류 (시스템)
    
    저장실패 --> draft : 사용자가 재시도
    saved --> [*] : 저장 완료
    
    note right of draft : 프론트엔드 로컬 상태\n실시간 입력 반영
    note right of 검증중 : 클라이언트 측 유효성 검사\n필수값, 숫자 형식 등
    note right of 저장중 : API 호출 진행\n영업관리 E~H,M 열 업데이트
```

## 4. DBOrder 상태 전이

```mermaid
stateDiagram-v2
    [*] --> 입력 : 사용자가 DB 주문 입력
    
    입력 --> 확정 : 주문 확인 (사용자 액션)
    입력 --> 임시저장 : 임시 저장 (사용자 액션)
    
    임시저장 --> 입력 : 수정 재개 (사용자 액션)
    임시저장 --> 확정 : 주문 확정 (사용자 액션)
    임시저장 --> 취소 : 주문 취소 (사용자 액션)
    
    확정 --> 진행중 : 시스템에서 처리 시작
    확정 --> 취소 : 사용자 취소 요청
    
    진행중 --> 완료 : DB 공급 완료 (시스템/관리자)
    진행중 --> 취소 : 공급 중단 (시스템/관리자)
    
    완료 --> [*] : 주문 완료
    취소 --> [*] : 취소 처리 완료
    
    note right of 입력 : DB관리 탭에서\n수량/채널 입력
    note right of 확정 : 주문 확정 시\n재고에서 차감
    note right of 완료 : 실제 DB 전달 완료\n효율 지표에 반영
```

## 상태 전이 트리거 요약

### 사용자 액션 트리거
- **Meeting**: 예약 생성, 완료 처리, 계약 입력, 취소 요청
- **Payment**: 승인건수 입력, 수납 확인, 금액 입력
- **DailyEntry**: 데이터 입력, 저장 요청, 오류 수정
- **DBOrder**: 주문 입력, 확정 처리, 취소 요청

### 시스템 이벤트 트리거
- **Meeting**: 업체관리 → 영업관리 수식 집계 (TEXTJOIN, COUNTIFS)
- **Payment**: 수납관리 → 영업관리 수식 집계 (SUMIFS), 기관 연동 (승인/반려)
- **DailyEntry**: 클라이언트 검증, API 응답
- **DBOrder**: 재고 관리 시스템, 관리자 처리

### 상태 전이 제약 조건
1. **Meeting**: 예약 → 완료 전환 시 미팅날짜/미팅시간 검증 필수
2. **Payment**: 승인 → 수납완료 시 금액 일치성 검증, 수납날짜 유효성
3. **DailyEntry**: 필수 필드 (date, channels) 검증, 영업관리 E~H,M 열만 쓰기
4. **DBOrder**: 재고 수량 확인 후 확정 처리

### 새로운 제약 조건 (ADR-0003)
1. **영업관리 I~L, N~T 수식 컬럼**: 웹에서 직접 쓰기 시도 시 400 에러
2. **예약일 vs 미팅날짜**: 예약일(생산 지표) ≠ 미팅날짜(컨택 지표) 구분 강제
3. **표시문자열 수식**: N열(날짜 포함), O열(날짜 제외) 자동 생성만