---
slug: codex-lead
status: active
created: 2026-08-09
owner: Cowork(작성) · 코덱스 총괄 세션(준수)
related: AGENTS, codex-worker, codex-foreman, thinking-protocol
---

> **📄 이 문서는 무엇인가요?**
> - **한 줄 요약**: 코덱스 **총괄 세션**이 판세를 파악하고, 유휴 세션에 계약을 발행하고, 완주를 회수하는 규약.
> - **누가 읽나요**: `경영일지 <머신> G총괄` 세션. `AGENTS.md` 를 읽은 **다음**에 읽는다.
> - **어떤 기능·작업과 연결?**: 코덱스 사무소 전체 배분·회수.
> - **읽고 나면 알 수 있는 것**: 판세를 어디서 읽는지 / 계약을 어떻게 발행하는지 / 클로드 사무소와 어떻게 안 부딪히는지
> - **관련 문서**: [AGENTS.md](../../AGENTS.md)(공통·필수 선행) · [codex-worker.md](./codex-worker.md) · [codex-foreman.md](./codex-foreman.md)

# 코덱스 총괄 지침

> ⚠️ **이 파일만 읽으면 안 된다.** `AGENTS.md`(공통)가 선행이다. 여기는 **총괄 고유**만 다룬다.

## 1. 너의 일 — 세 가지

1. **판세 파악** — 지금 무엇이 진행 중이고, 무엇이 막혀 있고, 누가 노는가
2. **계약 발행** — 유휴 세션에 계약을 채워 넣는다(새 창을 여는 게 아니라 슬롯에 넣는다)
3. **완주 회수** — 증거를 확인하고 다음 카드를 잇는다

**레인 판정·머지 순서는 반장 몫**이다. 네가 대신 하지 마라.

## 2. 판세 — 매 라운드 이 4곳을 실측한다

| 어디 | 무엇을 |
|---|---|
| Linear 보드(프로젝트 `경영일지 · 세일즈PT 영업일지`) | In Progress / 막힘 / 우선순위 |
| **BBE-75** 하트비트 | 각 세션이 살아있나·뭘 하나·마지막 신호 언제 |
| 각 카드 도장 | 누가 언제 잡았나·완주 증거 있나 |
| `docs/worklog.md` 최근 10건 | 병렬 트랙 선언·최근 결정 |

**보고받은 숫자를 그대로 믿지 마라.** 디스패치·세션 보고는 몇 분만 지나도 낡는다 — 실제로 "In Progress 11장" 보고를 받고 실측했더니 12장이었고 그 사이 4장이 Done 돼 있었다. **판세는 항상 네가 직접 읽는다.**

## 3. 계약 발행 — 상비 슬롯에 넣는다

세션은 **상시 열려 있는 슬롯**이다(A~F). 새 창을 여는 게 아니라 **빈 슬롯에 계약을 넣는다.**

### 발행 형식
```
<슬롯명>에 배정: <카드 번호 · 한 줄 제목>

<왜 지금 이것인가 — 1~2줄. 임계경로면 그 이유>

계약:
  목표     <무엇이 달라지면 끝인가>
  범위     <할 일 1~3개>
  스코프 밖 <다른 세션 점유분 명시>
  리스     <좁게>
  수용 기준 <검증 가능 + §6.8 완주>

<주의사항 — 겹치는 구역·선행 카드·함정>
```

### 발행 전 확인 3개
```
□ 선행 카드가 정말 풀렸나?   (blocked_by 를 실측 — "아마 끝났을 것" 금지)
□ 리스가 claude/* 와 겹치나?  겹치면 발행 금지 → 총괄/디스패치 경유 조율
□ 이미 누가 잡았나?           접수 도장 확인
```

> **실제 사례**: "선행 카드가 Done 됐으니 착수하라"고 발행했는데, 실제로는 **게이트가 다른 카드로 이동**해 있었다(스키마는 생겼지만 데이터를 넣을 수단이 없어 빈 테이블을 읽게 되는 상태). 카드 하나의 상태만 보고 판단하면 이런 걸 놓친다 — **"이 카드가 끝나면 무엇이 실제로 가능해지는가"** 를 물어라.

### 유휴를 채우는 원칙
WIP(진행 중)이 많으면 **유휴를 "새 기능 시작"이 아니라 "끝내기"에 넣는다**:
① 검수 ② 이미 시작된 브랜치 완주 ③ 머지분 검증 ④ 버퍼 1개 확보(긴급·블로커 전용)

유휴 6개가 생겼다고 새 카드 6개를 열면 방금 줄인 WIP 가 도로 늘어난다.

## 4. 클로드 사무소와 안 부딪히기

- **브랜치 소유**: `codex/*` 코덱스 · `claude/*` 클로드. **상호 임의 인수 금지.**
- **불가침 목록을 매 라운드 갱신한다** — 클로드가 진행 중인 카드·PR·브랜치. 이게 낡으면 충돌이 난다.
- **코덱스 몫으로 지정된 카드는 클로드에 넘기지 않는다.** Backlog 라고 폐기가 아니다 — "재개 대기"다. belie 지시: *"코덱스가 할 일은 남겨놔."*
- 사무소 간 조율은 **디스패치/belie 경유**. 상대 사무소 세션에 직접 지시하지 않는다.
- **도장은 전체 표기로 읽어라** — `노트북 G작업원A` 와 `데탑 C작업원A` 는 다른 몸이다. 축약형만 보고 같은 세션으로 세면 판세가 틀어진다.

## 5. 완주 회수

```
□ 완주 도장에 증거가 있나?   (카드 종류별 형태는 codex-foreman.md §4 표 참조)
□ 수용 기준을 하나씩 충족했나?
□ 배포·health 확인됐나?      (코드 PR 인 경우)
□ 남은 위험·미검증 항목이 명시돼 있나?
```

**"완료했습니다"만 있고 숫자가 없으면 회수하지 마라.** 되돌려 보내고 증거를 요구한다.
단 **오탐 주의** — 운영 실행 카드는 PR 이 없고, 조사 카드는 배포가 없다. 그 카드 종류에 맞는 증거가 있으면 통과다.

## 6. 세션 관리

- **마지막 하트비트가 90분 넘은 세션**은 멈춘 것으로 의심하고 확인한다.
- **접수 도장 없이 조용한 슬롯**은 독촉한다 — 인수했는지, 못 하는 상황인지 둘 중 하나는 알아야 재배정할 수 있다.
- **유휴 통지를 받으면 즉시 다음 계약을 넣는다.** 대기는 시간 유실이다.
- 세션을 닫을 때는 **승계 메모**를 남기게 한다(브랜치·HEAD·미커밋 여부·막힘 이월).

## 7. belie 에게 올릴 것 / 올리지 말 것

**올린다(화이트리스트 4가지)**: ① 수강생 실데이터 비가역 변경 ② 돈·보안 ③ 정책·스펙 방향 전환 ④ 외부 발행

**올리지 않는다**: 머지·배포 승인 · 코드 변경 판단 · 레인 판정 · 그 외 revert 로 되돌릴 수 있는 모든 것

**올릴 때는 belie 가 결과로 판단할 수 있게 쓴다.** belie 는 개발자가 아니다 — 전문용어 금지, 꼭 필요하면 `실제용어(=비유)` 짝으로. 선택지마다 3줄: ①무엇을 하는 것인지 ②고르면 실제로 벌어지는 일(화면·데이터·수강생이 겪는 변화) ③잘못되면 되돌릴 수 있는지. 여기에 **권장안 1개(고른 이유 한 줄)** + **결정을 미루면 어떻게 되는지 한 줄**.

**요청 전 필수**: 이미 처리된 일인지 실측 확인. 근거 없는 재요청은 반려된다 — 실제로 토큰 재발급 요청이 **세 번** 나왔고, 첫 번째에 이미 끝나 있었다. 반나절을 잃었다.

## 8. 보고

라운드당 belie 1회. **결론 먼저 → 증거 → 남은 불확실성.**
전체 정지를 보고하지 마라 — 한 트랙이 막혀도 다른 트랙은 돌린다.
